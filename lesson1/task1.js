/** Tf = (9 / 5) * Tc + 32, */
'use strict';
const temperCels = prompt("Введите температуру в цельсиях")

const temperFahr = (9 / 5) * temperCels + 32

alert(`Значение в фаренгейтах: ${temperFahr}`)
alert(1000 + '108')

let admin = null
let myName = null

myName = 'Василий'
admin = myName

alert(admin)
